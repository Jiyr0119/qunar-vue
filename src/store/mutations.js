export default {
	changeCity (state, city) {
      state.city = city;
    },
    changeCode (state, code) {
      state.code = code;
    },
    changePlayItem(state, playItem){
    	state.playItem = playItem;
    }
}

